[package]
name    = "raster-tile"
version = "0.1.0"
edition = "2024"

[dependencies]
geo       = { path = "../geo" }
inf       = { path = "../inf" }
thiserror = "2.0"
lz4_flex  = "0.11"

[dev-dependencies]
path_macro = "1.0"
geo        = { path = "../geo", features = ["gdal-static"] }

[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen = "0.2"
js-sys       = "0.3"
